<div class="container">
  <div class="row justify-content-center">
    <%= render 'shared/errors', obj: @category %>

    <%= form_with(model: @category, class: "shadow-sm p-3 mb-3 bg-info rounded") do |form| %>
      <div class="form-group row">
        <%= form.label :name, 'Category name', class: "col-2 col-form-label text-light" %>
        <div class="col-10">
          <%= form.text_field :name, class: "form-control shadow rounded", placeholder: "Enter a name" %>
        </div>
      </div>

      <div class="form-group text-center justify-content-center mt-2">
        <%= form.submit class: "btn btn-outline-light btn-lg" %>
      </div>
    <% end %>

    <%= link_to "[ Cancel and return to articles listing ]", articles_path, class: "text-center text-info mb-3" %>
  </div>
</div>
